{% if hermes_booking_installed|default(false) and hermes_booking_enabled|default(false) %}
<li class="nav-item mt-4">
    <a class="nav-link" href="{{ path('hermes_booking_admin_index') }}">{{ 'booking.admin.nav'|trans({}, 'booking') }}</a>
</li>
{% endif %}
